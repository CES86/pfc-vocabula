<% if(session.user.isAdmin){ %>
<p>
	<label for="origen">Lengua ORIGEN:</label>
	<select name="origen" required>
		<option value="">-</option>
		<% for(var i = 0; i < langues.length; i++) { %>
		<option value="<%= langues[i].DISTINCT %>"><%= langues[i].DISTINCT %>
		</option>
		<% } %>
	</select>
<p>
	<label for="destino">Lengua DESTINO:</label>
	<select name="destino" required>
		<option value="">-</option>
		<% for(var i = 0; i < langues.length; i++) { %>
		<option value="<%= langues[i].DISTINCT %>"><%= langues[i].DISTINCT %>
		</option>
		<% } %>
	</select>
<p>
	<% }else{ %>
<p>
	<label for="origen">Lengua ORIGEN:</label>
	<input type="radio" name="origen" value="<%= session.user.motherLang %>" required> <%= session.user.motherLang %>
	<input type="radio" name="origen" value="<%= session.user.foreignLang %>" required> <%= session.user.foreignLang %>
<p>
	<label for="destino">Lengua DESTINO:</label>
	<input type="radio" name="destino" value="<%= session.user.foreignLang %>" required> <%= session.user.foreignLang %>
	<input type="radio" name="destino" value="<%= session.user.motherLang %>" required> <%= session.user.motherLang %>
<p>
	<% } %>
<% include ../includes/_buttonsAdd.ejs %>