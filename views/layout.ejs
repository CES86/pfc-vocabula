<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>PFC-Vocabula</title>
	<script type="text/javascript">
		if (document.domain == "pfc-vocabula.herokuapp.com")
			;
		else{
			document.write("<meta http-equiv='refresh' content='15'/>");
			document.write("<link rel='stylesheet' href='/css/bootstrap.css'/>");
		}
	</script>
	<link rel='stylesheet' href='/css/style.css'/>
</head>
<body>
<div id="page-wrap">
	<header class="main" id="h1">
		<% if(!session.user){ %>
		<span class="right"><a href="/user/new">Registrarse</a> - <a href="/user/login">Login</a></span>
		<% }else{ %>
		<span class="right"> <%= session.user.username %> - <a href="/user/<%= session.user.username %>/edit">Editar</a> - <a
					href="/user/logout">Salir</a></span>
		<% } %>

		<h2>Bienvenid@ a <span>PFC-Vocabula</span></h2>
	</header>

	<nav class="main" id="n1" role="navigation">
		<span><a href="/">Inicio</a></span>
		<% if(session.user){ %>
		<span><a href="/user/<%= session.user.username %>">Perfil</a></span>
		<% } %>
	</nav>

	<section class="main" id="s1">
		<% if (errors.length) { %>
		<span id='ErrorMsgs'>
        Corregir errores:<br/>
			<% for (var i in errors) { %>
			<span> <%= errors[i].message %> </span><br/>
			<% } %>
		</span>
		<% } %>

		<div><%- body %></div>
	</section>

	<footer class="main" id="f1">
		<a href="http://es.wikipedia.org/wiki/Node.js">Acerca de...</a>
	</footer>
</div>
</body>
</html>
